<h1>All Tags</h1>

<ul id="articles">
    <% @tags.each do |tag| %>
        <li>
            <%= link_to tag.name, article_path(tag.articles.first.id), class: 'article_title' %>
        </li>
    <% end %>
</ul>

<%= link_to "Create a New Article", new_article_path, class: "new_article" %>